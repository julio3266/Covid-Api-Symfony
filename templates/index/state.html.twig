{% extends "base.html.twig" %}

{% block body %}
    <div class="row bg-dark">
        {% for state in states %}
            <div class="col-md-3 mr-auto ml-auto">
                <div class="card card-body mb-5 mt-3 text-light bg-dark text-center ">
                    <a class="stateLink h3" href="{{ path("index-city", {uf: state.uf}) }}">
                        {{ state.name }}
                    </a>
                    <p class="mt-3">
                      <strong>
                          Confirmados: {{ state.quantityConfirmed }}
                      </strong>
                    </p>
                    <p class="mt-2">
                        <strong>
                            Óbitos: {{ state.quantityDeaths }}
                        </strong>

                    </p>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}